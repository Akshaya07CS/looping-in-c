#include <stdio.h>
int main() {
    int sum = 0, i = 1, n;
    printf("Enter the value of n:");
    scanf("%d", &n);
    while (i <= n) {
        sum = sum + i;
        i = i + 1;
    }
    printf("The sum is %d\n", sum);
    return 0;
}
