#include <stdio.h>
int main() {
    int n, m, t, i, j;
    printf("Enter n:");
    scanf("%d", &n);

    m = (n / 2) + 1;
    t = m;

    for (i = 1; i <= t; i++) {
        for (j = 1; j <= i + (n / 2); j++) {
            if (j < m)
                printf("\t");
            else if (j >= m && j <= i + (n / 2))
                printf("\t*");
            else
                break;
        }
        m = m - 1;
        printf("\n");
    }

    m = m + 2;
    for (i = i - 2; i >= 1; i--) {
        for (j = 1; j <= i + (n / 2); j++) {
            if (j < m)
                printf("\t");
            else if (j >= m && j <= i + (n / 2))
                printf("\t*");
            else
                break;
        }
        m =
